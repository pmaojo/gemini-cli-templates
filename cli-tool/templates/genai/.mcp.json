{
  "mcpServers": {
    "gemini": {
      "command": "npx",
      "args": [
        "-y",
        "@google/gemini-mcp"
      ],
      "env": {
        "GEMINI_API_KEY": "${GEMINI_API_KEY}"
      }
    }
  }
}
