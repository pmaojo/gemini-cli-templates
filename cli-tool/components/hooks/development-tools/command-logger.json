{
  "description": "Log all Gemini CLI commands to a file for audit and debugging purposes. Simple logging that records tool usage with timestamps.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"[$(date)] Tool: $GEMINI_TOOL_NAME | File: $GEMINI_TOOL_FILE_PATH\" >> ~/.gemini/command-log.txt"
          }
        ]
      }
    ]
  }
}