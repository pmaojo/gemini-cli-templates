{
  "description": "Restrict authentication to Google Console accounts only. This ensures all usage is billed through the API billing system and prevents access via Gemini.ai accounts, ideal for enterprise environments with centralized billing.",
  "forceLoginMethod": "console"
}